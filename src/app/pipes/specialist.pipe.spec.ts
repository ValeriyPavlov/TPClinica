import { SpecialistPipe } from './specialist.pipe';

describe('SpecialistPipe', () => {
  it('create an instance', () => {
    const pipe = new SpecialistPipe();
    expect(pipe).toBeTruthy();
  });
});
