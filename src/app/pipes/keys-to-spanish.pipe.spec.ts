import { KeysToSpanishPipe } from './keys-to-spanish.pipe';

describe('KeysToSpanishPipe', () => {
  it('create an instance', () => {
    const pipe = new KeysToSpanishPipe();
    expect(pipe).toBeTruthy();
  });
});
